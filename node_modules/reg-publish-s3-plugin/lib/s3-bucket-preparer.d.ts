import { PluginPreparer, PluginCreateOptions, PluginLogger } from "reg-suit-interface";
import { PluginConfig } from "./s3-publisher-plugin";
export interface SetupInquireResult {
    bucketName: string;
}
export declare class S3BucketPreparer implements PluginPreparer<SetupInquireResult, PluginConfig> {
    _logger: PluginLogger;
    inquire(): {
        name: string;
        type: string;
        message: string;
    }[];
    prepare(config: PluginCreateOptions<SetupInquireResult>): Promise<PluginConfig>;
}
